<resources>

    <!-- este estilo lo utiliza la aplicacion al  inicializar los componentes por lo que no se puede modificar -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
         <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    
    <!-- estos estilos son para dar un formato unificado a todo el texto-->
    <style name="estilo_texto">
        <!-- indica que el color del texto va a sera azul-->
        <item name="android:textColor">#1075c2</item>
        <!-- indica el tamaño del texto-->
        <item name="android:textSize">15sp</item>
        <!-- indica el interlineado entre cada renglon del texto de la aplicacion-->
        <item name="android:paddingLeft">3dp</item>
    </style>
</resources>
